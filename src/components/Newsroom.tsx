import { View, Text } from "react-native";
import React from "react";

const Newsroom = () => (
    <View className="flex flex-1 items-center justify-center">
        <Text className="text-xl font-bold">Newsroom</Text>
    </View>
);

export default Newsroom;
