import { View, Text } from "react-native";
import React from "react";

const CoinList = () => (
    <View className="flex flex-1 items-center justify-center">
        <Text className="text-xl font-bold">Coin List</Text>
    </View>
);

export default CoinList;
